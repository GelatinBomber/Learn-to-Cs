// Visual Studio 사용법

// 주석 처리
    // CTRL + C + K 전체 주석 처리
    // CTRL + C + U 전체 주석 해제

// 디버깅
    // F9 정지 구간 설정
    // F10 정지 구간에서 실행
        // 이걸 통해서 반환된 값 등을 확인
    // F11 함수로 건너가기 ㅁ 아직 써보지는 않음


// Cleen Code Rule
    // if 안에는 가독성을 높이기 위해서 직접 식을 넣지 않은다.
        // 이를 해결하기 위해서는 bool을 이용해서 값을 할당한다.